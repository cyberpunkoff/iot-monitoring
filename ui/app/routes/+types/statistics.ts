import type { ActionFunctionArgs, LoaderFunctionArgs } from "react-router";

export const Route = {
  MetaArgs: {} as Record<string, never>,
  LoaderArgs: {} as LoaderFunctionArgs,
  ActionArgs: {} as ActionFunctionArgs,
}; 